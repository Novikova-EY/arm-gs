UPDATE [Доп_угли(архив)] INNER JOIN [Станции(архив)] ON ([Доп_угли(архив)].YEAR=[Станции(архив)].year) AND ([Доп_угли(архив)].NUMB1120=[Станции(архив)].numb1120) SET [Доп_угли(архив)].bikin = [Станции(архив)]!luch;
