SELECT [Станции(макет)].OES AS code, [Станции(макет)].YEAR, Sum([Станции(макет)].E) AS Сумма
FROM [Станции(макет)]
WHERE ((([Станции(макет)].VED)>0) And (([Станции(макет)].OES)=2))
GROUP BY [Станции(макет)].OES, [Станции(макет)].YEAR;
